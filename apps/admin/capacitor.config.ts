import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'social.sport.admin',
  appName: 'admin',
  webDir: '../../dist/apps/admin',
  bundledWebRuntime: false
};

export default config;
