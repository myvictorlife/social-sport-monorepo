import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'social-sport-rent',
  appName: 'rent',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
